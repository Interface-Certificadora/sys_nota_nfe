export interface Parceiro {
    id: number; 
    inativos: number; 
    nome: string; 
    email: string; 
    telefone: string; 
    chave_pix: string; 
    createdAt: string; 
    updatedAt: string; 
    ativos: number; 
    whatsapp: boolean;
}